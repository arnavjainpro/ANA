{
  "name": "ana",
  "version": "0.1.0",
  "private": true,
  "description": "Ana — a voice-first AI coding partner for non-technical people",
  "workspaces": [
    "apps/desktop",
    "apps/backend"
  ],
  "scripts": {
    "dev": "concurrently -n backend,desktop -c blue,green \"npm:dev:backend\" \"npm:dev:desktop\"",
    "dev:backend": "npm run dev --workspace apps/backend",
    "dev:desktop": "npm run dev --workspace apps/desktop",
    "build": "npm run build --workspace apps/backend && npm run build --workspace apps/desktop",
    "build:backend": "npm run build --workspace apps/backend",
    "build:desktop": "npm run build --workspace apps/desktop",
    "package": "npm run package --workspace apps/desktop",
    "typecheck": "npm run typecheck --workspace apps/backend && npm run typecheck --workspace apps/desktop"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  },
  "engines": {
    "node": ">=20"
  }
}
